---
weight: 4
title: '4. 可靠的请求 - 回复模式'
type: docs
---

# 第 4 章 - 可靠的请求 - 回复模式 {#reliable-request-reply}

[第 3 章 - 高级请求 - 回复模式](chapter3#advanced-request-reply) 介绍了 ZeroMQ 请求 - 回复模式的高级用法，并提供了工作示例。本章将探讨可靠性的一般问题，并在 ZeroMQ 核心请求 - 回复模式之上构建一组可靠的消息传递模式。

在本章中，我们重点关注用户空间请求 - 回复*模式*，即可重用的模型，帮助您设计自己的 ZeroMQ 架构：

* **懒惰海盗模式**：客户端侧的可靠请求 - 回复
* **简单海盗模式**：使用负载均衡的可靠请求 - 回复
* **偏执海盗模式**：带心跳的可靠请求 - 回复
* **管家模式**：面向服务的可靠队列
* **泰坦尼克模式**：基于磁盘/断开的可靠队列
* **双星模式**：主备服务器故障转移
* **自由职业者模式**：无代理的可靠请求 - 回复

## 什么是"可靠性"？ {#What-is-Reliability}

大多数谈论"可靠性"的人并不知道他们真正的意思。我们只能根据故障来定义可靠性。也就是说，如果我们能够处理一组明确定定和理解的故障，那么我们就对这些故障是可靠的。不多不少。因此，让我们看看在分布式 ZeroMQ 应用程序中可能出现的故障原因，按概率大致降序排列：

* **应用程序代码**是最糟糕的罪魁祸首。它可能崩溃并退出、冻结并停止响应输入、运行太慢无法处理输入、耗尽所有内存等等。

* **系统代码**——比如我们使用 ZeroMQ 编写的代理——可能因与应用程序代码相同的原因而死亡。系统代码*应该*比应用程序代码更可靠，但它仍然可能崩溃和烧毁，特别是如果它试图为慢客户端排队消息而耗尽内存。

* **消息队列**可能溢出，通常发生在学会粗暴处理慢客户端的系统代码中。当队列溢出时，它会开始丢弃消息。因此我们会得到"丢失"的消息。

* **网络**可能失败（例如，WiFi 被关闭或超出范围）。ZeroMQ 在这种情况下会自动重新连接，但在此期间，消息可能会丢失。

* **硬件**可能失败，并带走运行在该机器上的所有进程。

* **网络**可能以奇特的方式失败，例如，交换机上的某些端口可能死亡，网络的这些部分变得无法访问。

* **整个数据中心**可能被闪电、地震、火灾击中，或者更常见的电力或冷却故障。

要使软件系统对*所有*这些可能的故障都完全可靠是一项极其困难和昂贵的工作，超出了本书的范围。

因为上述列表中的前五种情况涵盖了 99.9% 的现实世界需求（根据我刚刚进行的一项高度科学研究，该研究还告诉我 78% 的统计数据是当场编造的，而且永远不要相信我们没有自己伪造的统计数据），这就是我们要研究的内容。如果您是一家大公司，有钱花在最后两种情况上，请立即联系我的公司！我海滩房子后面有一个大洞，等着变成高管游泳池。

## 设计可靠性 {#Designing-Reliability}

因此，为了把事情变得极其简单，可靠性就是"当代码冻结或崩溃时保持正常工作"，这种情况我们简称为"死亡"。然而，我们想要保持正常工作的不仅仅是消息。我们需要考虑每个核心 ZeroMQ 消息模式，看看如何使其在代码死亡时也能工作（如果我们能做到的话）。

让我们逐一来看：

* **请求 - 回复**：如果服务器死亡（在处理请求时），客户端可以弄清楚这一点，因为它不会得到回复。然后它可以发脾气放弃、等待稍后重试、找到另一个服务器等等。至于客户端死亡，我们现在可以将其视为"别人的问题"。

* **发布 - 订阅**：如果客户端死亡（已经获取了一些数据），服务器并不知道。发布 - 订阅不会从客户端向服务器发送任何信息。但客户端可以通过带外方式联系服务器，例如通过请求 - 回复，并询问"请重新发送我错过的所有内容"。至于服务器死亡，这超出了我们这里的范围。订阅者也可以自我验证它们运行得不是太慢，并在运行太慢时采取行动（例如，警告操作员并死亡）。

* **管道**：如果工人死亡（在处理任务时），上游节点可以弄清楚这一点，因为它不会得到确认。上游节点可以放弃并等待稍后重试、找到另一个工人等等。至于上游节点死亡，我们可以将其视为"别人的问题"。

现在让我们看看一些能够处理实际故障的可靠请求 - 回复模式。我们将逐步进行，从简单到复杂。

## 客户端可靠性（懒惰海盗模式） {#Client-Side-Reliability-Lazy-Pirate-Pattern}

正如海盗之歌所唱的："十六吨，你得到了什么？另一个日子变老，债务更深。"

我们从一个简单的客户端开始，使用 REQ 套接字进行请求 - 回复：

{{< examples name="lpclient" title="Lazy Pirate client" >}}

运行这个客户端，你会得到：

```
$ time ./lpclient
I: normal request (1)
I: normal request (2)
I: normal request (3)
I: normal request (4)
I: normal request (5)
I: normal request (6)
I: normal request (7)
I: normal request (8)
I: normal request (9)
I: normal request (10)

real    0m10.003s
user    0m0.000s
sys     0m0.008s
```

现在让我们让服务器崩溃。服务器会随机崩溃和重启，模拟各种故障。运行客户端，你会看到：

```
$ time ./lpclient
I: normal request (1)
I: normal request (2)
I: normal request (3)
W: no reply, reconnecting...
I: normal request (4)
I: normal request (5)
I: normal request (6)
I: normal request (7)
I: normal request (8)
I: normal request (9)
I: normal request (10)

real    0m12.527s
user    0m0.008s
sys     0m0.008s
```

客户端检测到了故障，重新连接，并重试了请求。这是懒惰海盗模式：客户端在放弃之前重试几次。

懒惰海盗模式的关键是：

* 使用 REQ 套接字，它会在发送下一条消息之前等待回复。
* 设置套接字超时（使用<tt>zmq_poll()</tt>或<tt>zmq_setsockopt()</tt>）。
* 在超时后重试请求。
* 在放弃之前重试几次。

这是懒惰海盗客户端的代码：

{{< examples name="lpclient" title="Lazy Pirate client" >}}

这是懒惰海盗服务器的代码：

{{< examples name="lpserver" title="Lazy Pirate server" >}}

## 基本可靠队列（简单海盗模式） {#Basic-Reliable-Queuing-Simple-Pirate-Pattern}

懒惰海盗模式适用于单个客户端 - 服务器对。但如果我们有多个客户端和多个服务器呢？我们需要一个代理来分配工作。让我们看看如何使代理可靠。

简单海盗模式使用一个代理来分配工作给多个工人。代理检测死亡的工人，并将工作重新分配给其他工人。

这是简单海盗模式的架构：

{{< textdiagram name="fig47.png" figno="47" title="Simple Pirate Pattern" >}}
     .---------.      .---------.      .---------
     |  Client |      |  Broker |      |  Worker |
     |   REQ   |      |  ROUTER |      |   REQ   |
     '----+----'      +----+----+      '----+----'
          |              |  |              |
          '--------------+--+--------------'
                         |  |
                         v  v
{{< /textdiagram >}}

简单海盗模式的关键是：

* 工人定期向代理发送心跳。
* 代理跟踪工人的心跳。
* 如果代理在一定时间内没有从工人那里收到心跳，它会认为工人死亡。
* 代理将死亡工人的工作重新分配给其他工人。

这是简单海盗代理的代码：

{{< examples name="spbroker" title="Simple Pirate broker" >}}

这是简单海盗工人的代码：

{{< examples name="spworker" title="Simple Pirate worker" >}}

## 健壮的可靠队列（偏执海盗模式） {#Robust-Reliable-Queuing-Paranoid-Pirate-Pattern}

偏执海盗模式是简单海盗模式的扩展，它增加了更复杂的心跳和重试逻辑。这个模式更健壮，可以处理更多的故障情况。

偏执海盗模式的关键是：

* 工人定期向代理发送心跳。
* 代理跟踪工人的心跳。
* 客户端定期向代理发送心跳。
* 代理跟踪客户端的心跳。
* 如果代理在一定时间内没有从工人或客户端那里收到心跳，它会认为它们死亡。
* 代理将死亡工人的工作重新分配给其他工人。
* 客户端可以重试失败的请求。

这是偏执海盗模式的代码：

{{< examples name="ppclient" title="Paranoid Pirate client" >}}

{{< examples name="ppworker" title="Paranoid Pirate worker" >}}

{{< examples name="ppbroker" title="Paranoid Pirate broker" >}}

## 心跳 {#Heartbeating}

心跳是可靠性的关键机制。它允许我们检测死亡的节点，并采取适当的行动。

在 ZeroMQ 中，心跳通常使用以下模式实现：

1. 节点定期发送心跳消息。
2. 接收节点跟踪最后收到心跳的时间。
3. 如果在一定时间内没有收到心跳，接收节点认为发送节点死亡。
4. 接收节点采取适当的行动（例如，重新分配工作，重试请求等）。

这是心跳的一个简单实现：

{{< examples name="heartbeat" title="Heartbeating" >}}

## 契约和协议 {#Contracts-and-Protocols}

在构建可靠的分布式系统时，定义清晰的契约和协议非常重要。这些契约指定了：

* 消息格式
* 消息序列
* 超时和重试策略
* 错误处理
* 心跳频率

例如，这是我们为海盗模式定义的契约：

* 工人必须定期发送心跳。
* 代理必须在一定时间内响应心跳。
* 客户端必须在一定时间内重试失败的请求。
* 所有消息必须符合特定的格式。

## 面向服务的可靠队列（管家模式） {#Service-Oriented-Reliable-Queuing-Majordomo-Pattern}

管家模式是偏执海盗模式的扩展，它添加了服务发现和负载均衡。这个模式允许我们构建面向服务的架构，其中客户端可以请求特定的服务，代理会将请求路由到适当的工人。

管家模式的关键是：

* 工人注册它们提供的服务。
* 客户端请求特定的服务。
* 代理将请求路由到提供该服务的工人。
* 代理跟踪每个服务的可用工人。
* 代理在提供相同服务的工人之间进行负载均衡。

这是管家模式的代码：

{{< examples name="mdcliapi" title="Majordomo client API" >}}

{{< examples name="mdcliapi2" title="Majordomo client API (v2)" >}}

{{< examples name="mdwrkapi" title="Majordomo worker API" >}}

{{< examples name="mdbroker" title="Majordomo broker" >}}

## 异步管家模式 {#Asynchronous-Majordomo-Pattern}

异步管家模式是管家模式的扩展，它允许异步处理请求。这个模式对于需要长时间运行的请求或需要并行处理多个请求的应用程序很有用。

异步管家模式的关键是：

* 客户端可以发送异步请求。
* 工人可以异步处理请求。
* 代理可以跟踪未完成的请求。
* 客户端可以在稍后时间获取回复。

这是异步管家模式的代码：

{{< examples name="mdcliapi2" title="Asynchronous Majordomo client API" >}}

{{< examples name="mdbroker2" title="Asynchronous Majordomo broker" >}}

## 服务发现 {#Service-Discovery}

服务发现允许客户端动态地发现可用的服务。这在服务可能来来去去的分布式系统中很有用。

在 ZeroMQ 中，服务发现可以使用以下模式实现：

1. 服务提供者定期宣布它们的服务。
2. 服务消费者监听这些公告。
3. 服务消费者缓存可用的服务。
4. 服务消费者根据需要联系服务提供者。

这是服务发现的一个简单实现：

{{< examples name="service-discovery" title="Service discovery" >}}

## 幂等服务 {#Idempotent-Services}

幂等服务是可以安全地多次调用的服务。这在分布式系统中很重要，因为请求可能会由于网络故障或其他问题而重复。

要使服务幂等，我们需要：

* 为每个请求分配唯一的 ID。
* 跟踪已处理的请求 ID。
* 忽略具有已处理 ID 的请求。
* 或者，确保多次处理相同的请求不会产生不同的结果。

这是幂等服务的一个示例：

{{< examples name="idempotent" title="Idempotent service" >}}

## 断开连接的可靠性（泰坦尼克模式） {#Disconnected-Reliability-Titanic-Pattern}

泰坦尼克模式允许我们在客户端和服务器断开连接时也能提供可靠性。这个模式将请求和回复存储在磁盘上，以便在连接恢复时可以处理它们。

泰坦尼克模式的关键是：

* 客户端将请求发送给泰坦尼克代理。
* 泰坦尼克代理将请求存储在磁盘上。
* 泰坦尼克代理将请求转发给实际的服务器。
* 泰坦尼克代理将回复存储在磁盘上。
* 客户端可以从泰坦尼克代理获取回复。

这是泰坦尼克模式的代码：

{{< examples name="titanic" title="Titanic pattern" >}}

泰坦尼克模式由以下组件组成：

1. **titanic.request**：存储客户端请求。
2. **titanic.reply**：存储服务器回复。
3. **titanic.close**：确认请求已完成。

客户端使用以下步骤与泰坦尼克模式交互：

1. 发送请求给 titanic.request。
2. 定期轮询 titanic.reply 以获取回复。
3. 收到回复后，发送 titanic.close。

## 高可用性对（双星模式） {#High-Availability-Pair-Binary-Star-Pattern}

双星模式提供了主备服务器故障转移。这个模式允许我们构建高可用性的服务，其中备份服务器可以在主服务器失败时接管。

双星模式的关键是：

* 两个服务器：主服务器和备份服务器。
* 主服务器处理请求。
* 备份服务器监视主服务器。
* 如果主服务器失败，备份服务器接管。
* 客户端可以连接到任一服务器。

这是双星模式的代码：

{{< examples name="bstar" title="Binary Star pattern" >}}

{{< examples name="bstarsrv" title="Binary Star server" >}}

{{< examples name="bstarcli" title="Binary Star client" >}}

双星模式的状态机如下：

1. **主服务器**：
   - 启动为主服务器。
   - 绑定前端和后端套接字。
   - 向备份服务器发送心跳。
   - 处理客户端请求。
   - 如果备份服务器死亡，继续运行。
   - 如果收到备份服务器的接管请求，关闭。

2. **备份服务器**：
   - 启动为备份服务器。
   - 连接到主服务器。
   - 接收来自主服务器的心跳。
   - 如果主服务器死亡，接管并成为主服务器。
   - 向客户端发送接管通知。

## 无代理可靠性（自由职业者模式） {#Brokerless-Reliability-Freelance-Pattern}

自由职业者模式提供了无代理的可靠请求 - 回复。这个模式允许客户端直接连接到多个服务器，并在服务器失败时进行故障转移。

自由职业者模式的关键是：

* 客户端维护一个服务器列表。
* 客户端按顺序尝试每个服务器。
* 如果服务器失败，客户端尝试列表中的下一个服务器。
* 客户端可以缓存服务器的状态。
* 客户端可以定期刷新服务器列表。

这是自由职业者模式的代码：

{{< examples name="flcliapi" title="Freelance client API" >}}

{{< examples name="flserver1" title="Freelance server 1" >}}

{{< examples name="flserver2" title="Freelance server 2" >}}

{{< examples name="flserver3" title="Freelance server 3" >}}

## 结论 {#Conclusion}

在本章中，我们探讨了 ZeroMQ 的可靠请求 - 回复模式。我们已经看到了如何：

* 使用懒惰海盗模式构建简单的可靠客户端。
* 使用简单海盗模式构建可靠的负载均衡系统。
* 使用偏执海盗模式构建健壮的可靠队列。
* 使用心跳检测死亡的节点。
* 定义清晰的契约和协议。
* 使用管家模式构建面向服务的架构。
* 使用异步管家模式处理异步请求。
* 使用服务发现动态发现服务。
* 使用幂等服务确保请求可以安全地重试。
* 使用泰坦尼克模式处理断开连接的情况。
* 使用双星模式构建高可用性系统。
* 使用自由职业者模式构建无代理的可靠系统。

这些模式为我们提供了构建能够处理各种故障的分布式系统所需的工具。它们展示了 ZeroMQ 的灵活性和强大功能。

在下一章中，我们将探讨 ZeroMQ 的高级发布 - 订阅模式，它们允许我们构建可扩展的消息传递系统。